# @oneworks/plugin-relay 0.1.0-beta.8

- Reconnect registered devices with the selected Relay account session when an older device token is no longer accepted, keeping signed-in desktop clients online without requiring another login.
