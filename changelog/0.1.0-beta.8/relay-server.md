# @oneworks/relay-server 0.1.0-beta.8

- Publish immutable build metadata from the health endpoint so production promotion can prove that the approved commit is serving traffic.
- Validate the real desktop login handoff, Admin application assets, configured SSO providers, and unauthenticated API boundary before a production deployment is accepted.
