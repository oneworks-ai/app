# Utils 0.1.0-alpha.0

- Install managed `@oneworks/plugin-*` packages through the bootstrap npm cache using npm-compatible package resolution, dependency installation, registry config, and registry fallback behavior.
- Add Coding Plan and Token Plan model service metadata, provider ids, endpoint detection, billing metadata, dedicated protocol base URLs, and static default model catalogs.
- Mark Kimi Code models and quota as API-backed provider actions while keeping its dedicated Coding Plan endpoint separate from Kimi Platform API.
