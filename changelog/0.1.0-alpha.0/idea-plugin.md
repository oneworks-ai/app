# One Works IDEA Plugin

- Add the IntelliJ Platform plugin shell under `apps/idea-plugin`.
- Register the `One Works` tool window and load the project-local Web UI through JCEF.
- Start one `oneworks web` runtime per IDE project with dynamic ports and IDE-system data/log isolation.
