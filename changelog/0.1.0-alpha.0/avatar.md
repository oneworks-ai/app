# Avatar

- Added the shared `@oneworks/avatar` package for pixel-rect SVG avatar rendering.
- Added the `assets/avatar` preview/export submodule and path-scoped GitHub Pages deployment trigger.
- Updated the client room avatar to use the shared avatar renderer.
