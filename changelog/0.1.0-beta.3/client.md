# @oneworks/client 0.1.0-beta.3

- Fix chat send status rendering so completed adapter CLI checks no longer leave the conversation showing a CLI preparation/install message while the assistant reply is still pending.
- Shorten the adapter CLI preparation text to a neutral check message.
