# One Works 0.1.0-beta.4

- Fix Electron desktop first-open workspace loading so opening a project from the launcher enters the workspace without requiring a window reload.
- Publish the CLI runtime event text fix so adapter CLI preparation reports a neutral check status in released runtime events.
- Release the packaged Electron app with the first-open workspace fix and matching beta bootstrap/runtime package sequence.
