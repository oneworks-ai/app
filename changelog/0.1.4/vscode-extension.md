# @oneworks/vscode-extension 0.1.4

- Publish the first stable One Works VS Code extension using the next unused Marketplace and Open VSX numeric identity after the historical prerelease versions `0.1.0` through `0.1.3`.
- Keep the GitHub VSIX, VS Code Marketplace package, and Open VSX package byte-identical and validate the stable manifest identity before distribution.
- Provide the One Works workspace integration and packaged browser-control companion flow from the coordinated stable source revision.
