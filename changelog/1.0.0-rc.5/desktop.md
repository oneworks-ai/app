# @oneworks/desktop 1.0.0-rc.5

- Restore the sandboxed preload bridge in packaged Desktop builds so the Launcher can open and create local projects, and fail Electron builds when unsupported runtime dependencies remain external.
