# One Works 0.1.0-beta.2

- Release the packaged Electron workspace startup overlay fix in `@oneworks/client` and the desktop app.
- Publish the `oneworks` bootstrap package on the beta channel so beta installs resolve the matching runtime sequence.
