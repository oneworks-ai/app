# Relay Server

- Add a validated, versioned device-only transport advertisement for Cloudflare Relay deployments. Browser login, passkey and OAuth keep using the public Relay origin.
- Add a Durable Object Hibernation WebSocket control endpoint with hash-only attachments and per-frame permission revalidation. Job persistence wakes connected devices without idle HTTP polling, while direct Worker bearer traffic avoids Pages Function proxy request duplication.
