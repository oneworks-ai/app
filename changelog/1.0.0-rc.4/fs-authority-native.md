# @oneworks/fs-authority-native 1.0.0-rc.4

- Keep the packaged filesystem authority available in signed and notarized macOS builds by sealing its exact post-sign arm64 and x64 native artifact sizes and SHA-256 hashes into the root application.
