# @oneworks/plugin-relay 0.1.0-beta.10

- Open Relay login directly when no account is available instead of showing an empty account popover.
- Identify the default login server and refresh availability automatically, with bounded retries for transient discovery failures.
- Let account document lists inherit the launcher surface while preserving selection indicators and document preview behavior.
