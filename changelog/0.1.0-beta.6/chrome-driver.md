# @oneworks/plugin-chrome-driver 0.1.0-beta.6

- Renamed the public Chrome extension, Chrome Web Store listing, popup, release, and downloadable packages to `OneWorks`.
- Kept the Settings capability entry as `External Browser` / `外部浏览器` and preserved the existing Store item ID, extension key, package identity, protocol fields, storage keys, and runtime identifiers.
