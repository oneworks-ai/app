# One Works 0.1.0-beta.6

- Add adapter-owned imports for model services and worktree environments, including Codex user and project `config.toml` providers, source-aware environment migration, and searchable adapter selection in configuration pages.
