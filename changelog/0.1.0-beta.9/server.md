# @oneworks/server 0.1.0-beta.9

- Prevented plugin runtime restarts when the host-managed development client changes, its build output is refreshed, or Vite creates a temporary timestamped config bundle.
