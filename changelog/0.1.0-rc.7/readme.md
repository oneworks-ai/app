# One Works 0.1.0-rc.7

- Align all One Works applications, runtime packages, channels, adapters, and plugins on the coordinated rc.7 release identity.
- Fail release planning when a selected store-bound VS Code prerelease would reuse an existing numeric registry version, while preserving same-release recovery reruns.
