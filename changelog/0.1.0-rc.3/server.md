# Relay Server

- Run the npm-installed Relay Server CLI from its compiled `dist` entry instead of TypeScript source, and verify the packed node_modules layout can print CLI help on Node 22.
- Build the shared Relay transport types package before Cloudflare Worker bundling in dev and production workflows.
