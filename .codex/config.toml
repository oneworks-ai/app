# Keep approval requests interactive, but route eligible requests from every
# newly loaded task in this trusted project to Codex's reviewer. This avoids
# leaving background tasks waiting for a person without expanding sandbox,
# network, command, or external-service permissions.
approval_policy = "on-request"
approvals_reviewer = "auto_review"
