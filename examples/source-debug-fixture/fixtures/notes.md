# Fixture Notes

Use this file to verify workspace file links and message rendering.

- Relative path: `fixtures/notes.md`
- Rule path: `.oo/rules/DEBUG.md`
- Smoke command: `node scripts/smoke.mjs`

The fixture intentionally avoids package dependencies so it can be used before installing anything inside the example workspace.
