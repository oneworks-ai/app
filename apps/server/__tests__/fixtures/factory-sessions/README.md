# Sanitized Factory session fixtures

These JSONL records were generated from the `@factory/droid-sdk` 0.7.0 `SessionStartEventSchema` and `FactoryDroidMessageSchema`, then sanitized by replacing the working directory with `FACTORY_FIXTURE_CWD` and using synthetic ids/content. They contain no settings, account data, tokens, or credentials.
