# Qwen Code 0.21.11 native history fixture

This fixture was generated on 2026-08-13 with the official `@qwen-code/qwen-code@0.21.11` npm release, an isolated `HOME`, `QWEN_HOME`, and `QWEN_RUNTIME_DIR`, and a local OpenAI-compatible test server.

Sanitization is limited to replacing the temporary project/runtime paths with `<QWEN_FIXTURE_CWD>` and `<QWEN_FIXTURE_RUNTIME>`, removing telemetry-only records, and omitting API request logs. Native session, message, tool-call, agent, and parent identifiers are retained so importer identity and relationship behavior can be tested. No credential value was written to these files.
